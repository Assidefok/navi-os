# 2026-04-01

- Navi OS tenia una incidència de pantalla en blanc al control UI. S'han aplicat diverses correccions de frontend i routing, i la més estable ha estat servir el frontend de 8100 com a build estàtica amb `server.js` via PM2 en lloc del servidor dev de Vite.
- Corregit un bug real a `navi-os/src/modules/Brain.jsx`: al breadcrumb del Memory Explorer es referenciava `file.pinned` en lloc de `selectedFile.pinned`, cosa que podia trencar la vista.
- `navi-os/src/App.jsx` s'ha reforçat amb fallback de rutes i càrrega lazy/Suspense per reduir errors de càrrega en entorns embeguts.
- `navi-os/ecosystem.config.cjs` s'ha canviat perquè `navi-os-vite` també arrenqui amb `server.js` al port 8100. PM2 guardat després dels canvis.
- A `OPS`, la capa principal s'ha redissenyat amb 4 mòduls principals: `Mission Control`, `Sessions`, `Cron` i `Activity`, deixant la resta d'eines com a utilitats secundàries.
- Les targetes de `OPS -> Cron` ara tenen estil tipus dashboard i mostren dades reals de cron: `scheduleKind`, `scheduleType`, `timezone` i `scheduleExpr/interval`, llegits de `~/.openclaw/cron/jobs.json` des de `navi-os/server.js`.
- `Brain` s'ha redissenyat completament amb 3 pestanyes principals: `Daily Briefs`, `Memory` i `Analytics`, amb layout de sidebar esquerra + visor gran a la dreta per a briefs i memòria.
